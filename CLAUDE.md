# GridFlow 开发规则

## 1. Git 提交规则
- 每完成一个功能或阶段性改动 → 立即 `git commit`
- 提交信息必须包含版本号（如 `v3.1.0: xxx`）
- 提交后 → 更新 README.md 相关描述

## 2. 版本号规则
- 重大功能/架构重构 → 大版本 +1（`v3.0.0 → v4.0.0`）
- 小功能/UI 优化/增强 → 次版本 +0.1（`v3.0.0 → v3.1.0`）
- Bug 修复 → 修订版 +0.01（`v3.1.0 → v3.1.1`）
- 版本号同步更新：`version_info.txt` + `app/i18n.py` + `README.md`

## 3. 分支规则
- `features` 分支：日常开发，新功能和 bug 修复
- `master` 分支：稳定发布
- **绝不**主动将 features 合并到 master，只有用户明确说"合并"才执行

## 4. 沟通规则
- 所有对话用中文
- 涉及架构方案 → 先讨论确认，再执行
- 非简单改动 → 先出方案，用户同意后再写代码

## 5. 代码规则
- 不改动程序逻辑，只动 UI 文本和样式
- 新功能遵循现有 Worker + QThread 信号模式
- 主题切换：`_apply_styles()` 连接 `theme_changed` 信号
- 语言切换：`_apply_lang()` 连接 `lang_changed` 信号
- 优先复用现有组件和模式
